
export interface PageInfo {
    id: string;
    originalIndex: number;
    rotation: number;
    thumbnailUrl: string;
}

export interface ImageInfo {
    id: string;
    name: string;
    thumbnailUrl: string;
    rotation: number;
}
